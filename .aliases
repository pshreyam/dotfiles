# Python aliases
alias vc='python3 -m venv venv'
alias va='source ./venv/bin/activate'
alias vd='deactivate'
alias uppip='pip install --upgrade pip'
alias vca='vc && va && uppip'
alias vcai='vca && pip install -r requirements.txt'

# Interactive command aliases
alias mv='mv -in'
alias cp='cp -in'
alias rm='rm -i'

# Manage dotfiles
function dotfiles() { 
    /usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME $@
}

# Get coloured output
alias diff='diff --color=auto'

# Restrict ping count to 4
alias ping='ping -c 4'

# Open config files

alias cb='nvim ~/.config/bspwm/bspwmrc'
alias cs='nvim ~/.config/sxhkd/sxhkdrc'
alias cpol='nvim ~/.config/polybar/config'
alias cdun='nvim ~/.config/dunst/dunstrc'
alias cpic='nvim ~/.config/picom/picom.conf'
alias crof='nvim ~/.config/rofi/config.rasi'
alias czat='nvim ~/.config/zathura/zathurarc'
alias cnvi='nvim ~/.config/nvim/init.vim'
alias calac='nvim ~/.config/alacritty/alacritty.yml'
alias bm='nvim ~/.scripts/bookmarks/bookmarks.json'

# Repos navigation
alias gr='cd ~/Downloads/repos'

function cr() {
    reply=$(echo "$(/bin/ls -1 ~/Downloads/repos)" | fzf)
    if [[ -z $reply ]];then
        echo "No repo selected!"
    else
        cd ~/Downloads/repos/$reply
    fi
}

# Some django aliases
alias rs='python3 manage.py runserver'
alias mi='python3 manage.py migrate'
alias mm='python3 manage.py makemigrations'
alias csu='python3 manage.py createsuperuser'

# Some Power Options aliases
alias sd='systemctl poweroff'
alias rb='systemctl reboot'
